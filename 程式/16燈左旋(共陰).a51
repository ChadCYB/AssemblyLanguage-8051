	ORG		00H
	MOV		P0,#00
	MOV		P1,#00
START:
	MOV		PSW,#00H
	MOV		A,#1
PORT0:
	MOV		P1,#0
	MOV		P0,A
	CALL		DELAY
	RLC		A
	JC		PORT1
	JMP		PORT0
PORT1:
	MOV		P0,#0
	RLC		A
	MOV		P1,A
	JC		START
	CALL		DELAY
	JMP		PORT1

DELAY:
	MOV		R5,#1
DL2:	MOV		R4,#100
DL1:	MOV		R3,#200
	DJNZ		R3,$
	DJNZ		R4,DL1
	DJNZ		R5,DL2
	RET