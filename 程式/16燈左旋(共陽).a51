	ORG		00H
	MOV		P0,#0FFH
	MOV		P1,#0FFH
START:
	MOV		PSW,#00H
	MOV		A,#0FFH
PORT0:
	MOV		P1,#0FFH
	MOV		P2,#0FFH
	MOV		P3,#0FFH
	MOV		P0,A
	CALL		DELAY
	RLC		A
	JC		PORT0
PORT1:
	MOV		P0,#0FFH
	MOV		P2,#0FFH
	MOV		P3,#0FFH
	RLC		A
	MOV		P1,A
	CALL		DELAY
	JC		PORT1
	JMP		START

DELAY:
	MOV		R5,#1
DL2:	MOV		R4,#100
DL1:	MOV		R3,#200
	DJNZ		R3,$
	DJNZ		R4,DL1
	DJNZ		R5,DL2
	RET
